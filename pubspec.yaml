name: quick_app_alert
description: A premium, highly customizable in-app alert dialog and top-end floating notification system for Flutter applications.
version: 1.0.0
repository: https://github.com/mrmodiji1999/QuickAlerts
issue_tracker: https://github.com/mrmodiji1999/QuickAlerts/issues

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.0.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
